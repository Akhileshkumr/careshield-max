{
  "name": "careshield-max",
  "private": true,
  "version": "1.0.0",
  "description": "D2C health insurance buy journey \u2014 quote, medical declaration, instant bind & issue",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down -v",
    "db:generate": "npm run -w @careshield/api db:generate",
    "db:migrate": "npm run -w @careshield/api db:migrate",
    "db:studio": "npm run -w @careshield/api db:studio",
    "build:contracts": "npm run -w @careshield/contracts build",
    "build": "npm run build:contracts && npm run -w @careshield/api build && npm run -w @careshield/web build",
    "dev": "npm run build:contracts && concurrently -n contracts,api,web -c gray,cyan,magenta \"npm:dev:contracts\" \"npm:dev:api\" \"npm:dev:web\"",
    "dev:contracts": "npm run -w @careshield/contracts dev",
    "dev:api": "npm run -w @careshield/api dev",
    "dev:web": "npm run -w @careshield/web dev",
    "test": "npm run build:contracts && npm run -w @careshield/api test && npm run -w @careshield/web test",
    "test:unit": "npm run build:contracts && npm run -w @careshield/api test:unit",
    "test:e2e": "npm run build:contracts && npm run -w @careshield/api test:e2e",
    "typecheck": "npm run build:contracts && npm run -w @careshield/api typecheck && npm run -w @careshield/web typecheck",
    "start": "concurrently -n api,web -c cyan,magenta \"npm:start:api\" \"npm:start:web\"",
    "start:api": "npm run -w @careshield/api start",
    "start:web": "npm run -w @careshield/web start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^6.0.3"
  }
}
