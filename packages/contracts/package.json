{
  "name": "@careshield/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Types, zod schemas and exact-money helpers shared by the API and the web app",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
